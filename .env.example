VITE_API_URL=/api
VITE_USE_MOCK=false

# IMPORTANTE (Vercel):
# Use sempre VITE_API_URL=/api
# Nao aponte direto para o Render (causa CORS no browser).
# O vercel.json faz proxy: /api/* → https://chatbo-backendagent.onrender.com/api/*

# Logs do browser sao enviados para /api/client-logs (Serverless Function)
# e aparecem em Vercel → Project → Logs (apenas em PROD/preview)

# Agente de vendas WhatsApp (Z-API) — servico separado, nao e o backend PulseDesk
# https://agent-ia-xnamai.onrender.com/status
# Webhook Z-API: https://agent-ia-xnamai.onrender.com/webhook
